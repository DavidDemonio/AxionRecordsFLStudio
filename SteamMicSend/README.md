# SteamMic Send

VST3 effect for FL Studio that passes the insert audio through unchanged while sending a parallel PCM stream to the Windows playback endpoint named **Steam Streaming Microphone**. Discord can then use the corresponding Steam Streaming Microphone recording endpoint as its input.

The plugin performs no compression, limiting, EQ, gain control, or other signal processing.
